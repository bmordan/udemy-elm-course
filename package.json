{
  "scripts": {
    "serve": "elm-reactor --port=3000",
    "watch": "onchange *.elm -- elm-make todo-list.elm --output js/bundle.js",
    "start": "npm run watch && npm run serve"
  },
  "dependencies": {
    "onchange": "^3.3.0"
  }
}
